#include<stdio.h>
#include "holberton.h"

/**
 *main - prints lowercase english letters 10 times
 *
 *Return - always return 0
 */

int main(void)
{
int count = 1;
int ch;

while (count <= 10)
{
count++;
for (ch = 'a'; ch <= 'z'; ch++)
_putchar(ch);
_putchar('\n');
}

return (0);
}
